# Ultralytics YOLO dataset config
# Generated by prepare_dataset.py — paths are relative to repo root

path: ../outputs/detection/dataset
train: train/images
val: val/images

names:
  0: attached-energy
  1: attached-item
  2: card
  3: multicard
